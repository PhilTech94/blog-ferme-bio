<article class="article-card">
    <h2>{{ article.title }}</h2>
    <p class="article-date">{{ article.createdAt|date('d/m/Y') }}</p>
    <p class="article-excerpt">{{ article.content|slice(0, 150) }}...</p>
    <a href="#">Lire la suite</a>
</article>


